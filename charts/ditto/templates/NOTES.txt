Eclipse Ditto {{ .Chart.AppVersion }} deployed.

ESO mode: gateway.useExternalSecret={{ .Values.gateway.useExternalSecret }}
ESO mode: dbconfig.useExternalSecret={{ .Values.dbconfig.useExternalSecret }}

When using ESO (useExternalSecret: true), ensure secrets exist before sync:
  Gateway secret: {{ .Values.gateway.existingSecretName | default "N/A" }}
  DB config secret: {{ .Values.dbconfig.existingSecretName | default "N/A" }}
